# 未央静态站

`index.html` 是未央入口页，样式直接由 `assets/css/main.css` 提供，不依赖构建步骤或第三方脚本。

- `images/bg.webp`：页面实际加载的背景图。
- `images/bg.jpg`：保留的原始源图。
- `info.html`、`redstone.html`、`build.html`、`joinus.html`：保留既有 URL，并继续跳转到城镇建设中页面。

本地检查时，从仓库根目录启动静态服务器后访问 `/towns/Fctinue/`。
